syntax="proto2";
package protocol;

message port_mapping {
  required uint32 localPort = 1;
  required uint32 remotePort = 2;
}

message register {
  required string clientId = 1;
  map<uint32, port_mapping> mapping = 2;
}
